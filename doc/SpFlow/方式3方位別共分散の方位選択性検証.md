# 方位別遅延snapshot方式 方位別共分散の方位選択性検証

## 1. 目的

方位別遅延snapshot方式のWeight方式を実装・校正する前に、方式が前提としていた次の仮説を検証する。

> 正しい候補方位では方位別共分散が強くなり、誤った候補方位では弱くなる。

受波信号に40度の信号が含まれる場合、120度候補で切り出したsnapshotから信号そのものが消えるわけではない。したがって、方位別共分散を単純な`信号あり／なし`として解釈せず、次を分離して評価する。

- snapshotに信号powerが含まれているか
- 共分散がrank-1信号空間を持つか
- 信号の複素空間構造が候補方位のsteeringと整合するか
- 候補方位ごとに異なる時間sampleを使うことでcoherenceが変化するか

本検証では固定Weight閾値を決定しない。各指標の方位選択性を分類し、Weightの主指標と補助条件を確定することを目的とする。

## 2. 方位別遅延snapshot方式の方位別snapshot生成

### 2.1 配列shape

主な配列shapeは次のとおりとする。

| 配列 | shape | axis |
|---|---|---|
| 受波信号 | `[n_ch,n_sample]` | channel、時間sample |
| 中心sample表 | `[2,n_ch,n_beam]` | 1秒segment、channel、segment内beam |
| 方位一致表 | `[2,n_beam]` | 1秒segment、segment内beam |
| snapshot | `[n_ch,NFFT,n_beam]` | channel、snapshot時間、beam |
| FFT spectrum | `[n_ch,n_bin,n_beam]` | channel、周波数bin、beam |
| 方位別共分散 | `[n_direction,n_ch,n_ch,n_bin]` | 候補方位、行channel、列channel、周波数bin |
| steering table | `[n_ch,n_bin,n_direction]` | channel、周波数bin、候補方位 |

評価条件では`n_ch=64`、`NFFT=128`、実信号rFFTなので`n_bin=65`、global保持方位数は159である。各1秒は片側79方位を各2回と90度を1回観測し、合計159 snapshotとする。偶数秒・奇数秒で左右を切り替え、非更新側は減衰させず保持する。

### 2.2 中心sampleと時間軸復元

候補方位ごとに、到来遅延へ対応した異なる中心sampleから各channelのsnapshotを取得する。channel `i`、候補方位 `θ` の中心sampleを`c_i(θ)`、共通参照中心を`c_ref(θ)`とすると、中心時刻差は次式となる。

\[
\Delta t_i(\theta)
=
\frac{c_i(\theta)-c_{\mathrm{ref}}(\theta)}{f_s}
\]

異なる中心時刻から得たFFT spectrumを共通参照時刻へ戻すため、FFT後に次の位相補正を行う。

\[
X_i^{\mathrm{ref}}(k,\theta)
=
X_i^{\mathrm{cut}}(k,\theta)
\exp\left(-j2\pi f_k\Delta t_i(\theta)\right)
\]

負号は、時刻が`Δt`だけ進んだsnapshotに含まれる正の位相進みを打ち消し、共通参照時刻へ戻す規約に対応する。単位は`f_k [Hz]`、`Δt [s]`であり、指数部は無次元である。

この補正後に形成する共分散は、候補方位へ整相済みの座標ではなく、共通参照時刻に戻した物理的なchannel位相を保持する。

\[
R(k,\theta)
=
E\left[
X^{\mathrm{ref}}(k,\theta)
X^{\mathrm{ref}}(k,\theta)^H
\right]
\]

したがって、steering整合評価では物理steering vectorを一度適用する。全channel同相vectorを使うのは、候補方位へ整相済みの共分散を評価する別の定義であり、現行方位別遅延snapshot方式には適用しない。

## 3. 方位差が生じる条件

### 3.1 対角unitary変換だけの場合

候補方位処理がchannelごとの位相回転だけなら、方位別共分散は次式で表される。

\[
R_\theta=D(\theta)R D(\theta)^H
\]

`D(θ)`が絶対値1の対角要素を持つunitary行列なら、次の量は候補方位によらず不変である。

\[
\operatorname{tr}(R_\theta)=\operatorname{tr}(R)
\]

\[
\lambda_i(R_\theta)=\lambda_i(R)
\]

\[
\lVert R_\theta\rVert_F=\lVert R\rVert_F
\]

\[
\left|(R_\theta)_{ij}\right|=|R_{ij}|
\]

従って、trace、固有値、Frobenius norm、絶対値正規化相関は方位を識別できない。

### 3.2 非unitaryな差が残る場合

方位別遅延snapshot方式では候補方位ごとに異なる有限時間窓を切り出す。そのため、次の場合は位相回転だけでは表せない差が残る可能性がある。

- 候補方位間の遅延差が信号の自己相関時間に対して無視できない
- 信号が広帯域でcoherence timeが短い
- snapshot長が短く、異なる窓が異なる波形区間を含む
- 長基線により候補方位間のchannel遅延差が大きい
- transientまたは時間変動信号を処理する

一方、定常tone、snapshotより十分長いcoherence time、短基線、小さい方位差ではunitary変換へ近づく。広帯域で指標に方位差が現れても、その差が正しい方位で最大になる保証はないため、実測で確認する。

## 4. 評価指標

### 4.1 共分散power

\[
P_{\mathrm{trace}}(k,\theta)
=
\operatorname{tr}(R(k,\theta))
=
\sum_i R_{ii}(k,\theta)
\]

`R_ii`はchannel `i`のpowerなので、traceは全channelの合計powerを表す。単位は入力信号の二乗にFFTスケールが掛かったpowerである。本評価では候補方位間の相対比較に使い、物理音圧レベルとして扱わない。

### 4.2 off-diagonal正規化相関

対角成分を除く下三角channel pairについて次を計算する。

\[
\rho_{ij}(k,\theta)
=
\frac{|R_{ij}(k,\theta)|}
{\sqrt{R_{ii}(k,\theta)R_{jj}(k,\theta)}}
\quad i>j
\]

複素相関を直接平均せず、各pairの絶対値正規化相関を求めてからmean、median、95 percentile、maximumを計算する。maximumは多数pairから最大値を選ぶ極値biasを持つため比較用に限定する。

非等間隔アレイの基線長はchannel index差ではなく受波器座標から求める。

\[
d_{ij}=\lVert p_i-p_j\rVert
\]

物理基線長別、波長正規化基線長`d_ij f_k/c`別、中央--中央・中央--外側・外側--外側pair別の統計も保存する。

### 4.3 複素steering整合量

物理steering vectorをchannel norm 1へ正規化する。

\[
u(k,\theta)
=
\frac{a(k,\theta)}{\lVert a(k,\theta)\rVert}
\]

共分散powerに対する候補steering方向のpower占有率を次式で定義する。

\[
\eta(k,\theta)
=
\frac{u(k,\theta)^H R(k,\theta)u(k,\theta)}
{\operatorname{tr}(R(k,\theta))}
\]

`η`は無次元で、理論範囲は`0--1`である。空間白色雑音では期待値が概ね次式となる。

\[
\eta_{\mathrm{noise}}\approx\frac{1}{N_{\mathrm{ch}}}
\]

64chでは`1/64=0.015625`である。

### 4.4 固有値集中度

共分散の固有値を降順に`λ1,λ2,...`とする。

\[
q_{\mathrm{eig}}(k,\theta)
=
\frac{\lambda_1(k,\theta)}{\operatorname{tr}(R(k,\theta))}
\]

\[
g_{\mathrm{eig}}(k,\theta)
=
\frac{\lambda_1(k,\theta)-\lambda_2(k,\theta)}
{\operatorname{tr}(R(k,\theta))}
\]

これらはrank-1信号空間の有無を表す補助指標であり、主固有vectorが候補steeringと一致することは保証しない。

### 4.5 rank-1 steering model残差

norm 1のsteering `u`に対する最適powerを次式で求める。

\[
p(k,\theta)=u(k,\theta)^H R(k,\theta)u(k,\theta)
\]

\[
R_{\mathrm{model}}(k,\theta)
=
p(k,\theta)u(k,\theta)u(k,\theta)^H
\]

\[
\epsilon(k,\theta)
=
\frac{\lVert R(k,\theta)-R_{\mathrm{model}}(k,\theta)\rVert_F}
{\lVert R(k,\theta)\rVert_F}
\]

`ε`は小さいほど候補steeringのrank-1 modelへ整合する。実装では巨大なmodel配列を追加生成せず、次の同値式を使う。

\[
\lVert R-puu^H\rVert_F^2
=
\lVert R\rVert_F^2-p^2
\]

## 5. steering整合量の同値計算

瞬時共分散を`R_inst=XX^H`とすると、次が成り立つ。

\[
u^H(XX^H)u=|u^H X|^2
\]

\[
\operatorname{tr}(XX^H)=\lVert X\rVert^2
\]

従って、FFT直後のsnapshotからsteering powerとtotal powerを計算し、共分散と同じ忘却係数で指数積分できる。

\[
P_{s,t+1}=(1-\alpha)P_{s,t}+\alpha|u^H X_t|^2
\]

\[
P_{t,t+1}=(1-\alpha)P_{t,t}+\alpha\lVert X_t\rVert^2
\]

\[
\eta=\frac{P_s}{P_t}
\]

完成共分散へ全方位の二次形式を適用する演算量は、

\[
O(N_{\mathrm{beam}}N_{\mathrm{bin}}N_{\mathrm{ch}}^2)
\]

FFT直後の直接射影は、

\[
O(N_{\mathrm{beam}}N_{\mathrm{bin}}N_{\mathrm{ch}})
\]

である。この削減は近似ではなく数学的同値変換である。

## 6. 評価条件

### 6.1 アレイと信号処理条件

| 項目 | 設定 |
|---|---:|
| array | 中央密・外側疎の左右対称64ch sparse array |
| 開口長 | 32 m |
| 最小隣接間隔 | 0.25 m |
| 最大隣接間隔 | 1.0 m |
| 音速 | 1500 m/s |
| sample rate | 8192 Hz |
| NFFT | 128 sample |
| snapshot長 | 15.625 ms |
| 周波数分解能 | 64 Hz |
| broadband帯域 | 128--1024 Hz |
| target方位 | 40 deg |
| target level | 0 dB re input RMS |
| noise level | -32 dB re input RMS/√Hz |
| 積分時間 | 10 s |
| 全方位の平均更新率 | 1 update/s |
| 10秒の概算有効snapshot数 | 10 |

target levelは帯域積分RMS、noise levelはone-sided amplitude spectral densityとしてscene_rendererへ指定する。toneの0 dB re input RMSは実信号peak振幅`√2`に対応する。

### 6.2 scene

次の6 sceneを独立生成した。

1. noise-only
2. 512 Hz tone target-only
3. 128--1024 Hz broadband target-only
4. broadband target + 空間白色雑音
5. broadband target + 空間相関雑音
6. 40度と75度の複数broadband信号 + 空間白色雑音

乱数seed、受波器全座標、SL、NL、音速、snapshot長、積分時間は成果物JSONとNPZへ保存する。

### 6.3 表示・数値比較条件

画像は全sceneで同じ方位軸、周波数軸、snapshot時刻を使用する。無次元比は色範囲`0--1`、traceは全scene共通最大値で正規化する。toneの数値評価は512 Hz binだけ、broadbandは128--1024 Hzを帯域平均する。

各指標について次を保存する。

- 正しい40度の補間値
- 40度±2度の平均
- 40度から20度以上離れた方位の平均
- 正解近傍対遠方margin
- peak方位とpeak誤差
- backgroundからpeakまでのhalf-height幅
- 正解方位外の最大値または最小残差
- 2秒完成周期ごとの時間収束

## 7. 決定論的テスト

次を小規模配列で確認した。

1. 対角unitary変換でtrace、固有値、Frobenius norm、`|R_ij|`が不変である。
2. rank-1共分散では整合steeringの`η=1`、直交steeringの`η=0`となる。
3. 4ch空間白色共分散では`η=1/4`となる。
4. 4ch空間白色共分散のrank-1残差は`√3/2`となる。
5. 同じsnapshotと忘却係数なら、完成共分散から求めたηとFFT直後に直接積分したηが一致する。

6 scene評価で直接積分ηと共分散ηの最大絶対差は`4.8×10^-7`以下であった。

## 8. 検証結果

### 8.1 主要数値

| scene | 指標 | 40度±2度 | 20度以上遠方 | margin | peak方位 |
|---|---|---:|---:|---:|---:|
| tone target-only, 512 Hz | trace | 226904 | 226904 | 0.0 | 0.00 deg |
| tone target-only, 512 Hz | correlation mean | 1.000 | 1.000 | 0.000 | 0.00 deg |
| tone target-only, 512 Hz | eta | 0.974 | 0.0038 | 0.970 | 39.87 deg |
| broadband target-only | trace | 28341 | 29911 | -1570 | 125.32 deg |
| broadband target-only | correlation mean | 0.837 | 0.760 | 0.078 | 51.27 deg |
| broadband target-only | eigenvalue fraction | 0.845 | 0.776 | 0.070 | 51.27 deg |
| broadband target-only | eta | 0.777 | 0.017 | 0.760 | 39.87 deg |
| broadband target-only | rank-1 residual | 0.385 | 0.995 | 0.610低下 | 39.87 deg |
| broadband + white noise | eta | 0.465 | 0.015 | 0.450 | 38.73 deg |
| broadband + correlated noise | eta | 0.464 | 0.029 | 0.436 | 39.87 deg |
| multiple signals | eta | 0.316 | 0.032 | 0.284 | 39.87 deg |

noise-onlyの128--1024 Hzにおけるη平均は`0.015701`であり、理論値`1/64=0.015625`との差は`0.000076`であった。

### 8.2 狭帯域tone

512 Hz toneでは、trace、絶対値相関、最大固有値比、固有値gapが候補方位によらず同一となった。これは定常狭帯域信号において候補処理がunitary位相変換へ近づくという予測と一致する。

一方、ηは39.87度にpeakを持ち、正解近傍と遠方のmarginは0.957であった。信号powerやrank-1性だけでは方位を区別できず、複素steeringとの整合が必要である。

### 8.3 広帯域信号

広帯域では候補ごとに異なる有限時間窓の効果により、trace、絶対値相関、固有値集中度にも差が現れた。しかし、次の理由から方位Weightには使用できない。

- target-onlyのtraceは正解近傍より遠方平均の方が大きい
- correlation meanと固有値比のpeakは51.84度となり、正解40度を示さない
- target + noiseではこれらのpeakが29--75度へ移動する
- 差の大きさが少数snapshotと有限窓のpower変動に依存する

ηは全target sceneで39.30--40.44度にpeakを形成した。空間相関雑音を加えても正解対遠方marginは0.428であった。

### 8.4 snapshot不足

全方位の平均更新率は1 update/sなので、10秒で概算10 snapshotを持つ。noise-onlyでも絶対値相関meanは約0.36、最大固有値比は約0.33となり、有限snapshotのsample covariance biasは残る。

broadband target-onlyのη peakは完成時刻2、4、6、8、10秒でそれぞれ次のように変化した。

| 完成時刻 | peak方位 |
|---:|---:|
| 2 s | 40.44 deg |
| 4 s | 38.73 deg |
| 6 s | 41.01 deg |
| 8 s | 39.30 deg |
| 10 s | 40.44 deg |

10秒条件では有限snapshotによる揺れが残る。T=10 sは実装同値性の確認に使い、T=40/128 sで分布収束を比較した結果、最終peakはいずれも39.87度であった。

### 8.5 開口長と共通信号の切り分け

本書では「共通信号」を、同一位相平面上にあり、物理的な到来遅延を補償すれば各channelで同一となる広帯域波形と定義する。正横90度から到来する場合は各channelの同一時刻の波形が共通成分となる。方位がずれる場合は、方位と受波器位置で決まる時間差だけ移動した波形が共通成分である。

開口長`D`が短いと、候補方位間の端間遅延差

\[
\Delta\tau_{\mathrm{mismatch}}
=
\frac{D\left|\cos\theta_s-\cos\theta_c\right|}{c}
\]

が広帯域信号の自己相関時間より十分小さくなる。このとき全候補方位のsteering vectorが事実上区別できず、etaが全方位で1に近づく。帯域幅`B=1024-128=896 Hz`に対する自己相関時間の目安は`1/B=1.116 ms`である。

中央密・外側疎の64ch相対配置を保ち、開口長だけを0.25--320 mへscaleした。方位量子化誤差を開口長効果と混同しないため、信号方位は159方位表上の39.8734度とした、128--1024 Hz、target-only、`T=10 s`である。

| 開口長 | 40度と90度の端間遅延差 | 遅延差 / `1/B` | 正解近傍eta | 20度以上遠方eta | margin | 全方位最小eta |
|---:|---:|---:|---:|---:|---:|---:|
| 0.25 m | 0.128 ms | 0.114 | 0.9998 | 0.9689 | 0.0309 | 0.9315 |
| 0.5 m | 0.255 ms | 0.229 | 0.9994 | 0.8894 | 0.1100 | 0.7661 |
| 1 m | 0.511 ms | 0.458 | 0.9973 | 0.6968 | 0.3006 | 0.4379 |
| 2 m | 1.021 ms | 0.915 | 0.9922 | 0.4563 | 0.5359 | 0.1760 |
| 4 m | 2.043 ms | 1.830 | 0.9793 | 0.2541 | 0.7253 | 0.0475 |
| 8 m | 4.086 ms | 3.661 | 0.9516 | 0.1122 | 0.8394 | 0.0079 |
| 16 m | 8.171 ms | 7.321 | 0.9051 | 0.0420 | 0.8632 | 0.0025 |
| 32 m | 16.342 ms | 14.643 | 0.7903 | 0.0171 | 0.7732 | 0.0012 |
| 320 m | 163.725 ms | 146.698 | 0.1518 | 0.0130 | 0.1388 | 0.0040 |

0.25 m開口では最小etaでも0.9315であり、「開口長が短いと全方位の相関が1に近づく」ことを再現した。ただし、320 mで正解etaが0.1518まで低下した原因は開口長の物理効果だけではない。中心sample表の行別scaleが到来遅延まで縮小している。

| 開口長 | 理論両端遅延 | 中心表snapshot 1 | 中心表snapshot 2 | 絶対値の不足 |
|---:|---:|---:|---:|---:|
| 32 m | -16.373 ms | -4.395 ms | -4.639 ms | 11.98 / 11.73 ms |
| 320 m | -163.725 ms | -44.678 ms | -46.021 ms | 119.05 / 117.70 ms |

したがって320 mの方位別共分散は、意図した「同一波面の同一時間区間を切り出した共分散」になっていない。中心表の遅延保持誤差を修正するまで、32 mおよび320 mの絶対etaを開口長の採否根拠に使用しない。

320 mの共分散はHermitian相対誤差`3.85e-8`、帯域内最小固有値`-5.57e-4`（最大固有値`2.79e4`に対する数値誤差範囲）で、行列の数値品質は成立した。しかし、traceの正解対遠方marginは`-2378`、相関meanは`0.0114`、固有値集中度は`0.0075`、steering etaは`0.1388`である。steering etaだけが正解39.8734度にpeakを保ったが、rank-1残差は正解近傍で0.949と大きく、正解方位でも共通snapshotを形成できていないことを示す。

![開口長ごとのsteering整合量](../../artifacts/beamforming/aperture_steering_directionality/aperture_eta_overlay.png)

### 8.6 100 Hz帯・長大等間隔ULAの再評価

320 m評価で見つかった行別scaleを廃止し、全channel共通の基準中心だけを可用範囲へscaleし、物理到来遅延をscaleせず加える実装へ修正した。

エンドファイアで候補方位間の時間軸差を十分に確保するため、64ch等間隔ULAを使用した。信号帯域は80--120 Hz、受波器間隔は120 Hzの半波長`6.25 m`、開口長`393.75 m`である。`fs=1024 Hz`、`NFFT=128`なのでブロック長は125 ms、エンドファイア両端遅延は262.5 msで、ブロック長の2.1倍である。

修正後の中心表が与えた両端遅延は2 snapshotとも`-262.695 ms`で、理論`-262.500 ms`との差は0.2 sampleであった。

| scene | 指標 | 正解0--2度 | 20度以上遠方 | margin | peak誤差 |
|---|---|---:|---:|---:|---:|
| target-only | correlation mean | 0.9999 | 0.6767 | 0.3232 | 1.139度 |
| target-only | steering eta | 0.9998 | 0.0030 | 0.9968 | 1.139度 |
| target-only | eigenvalue fraction | 0.9999 | 0.6939 | 0.3060 | 1.139度 |
| target-only | rank-1 residual | 0.0102 | 0.9996 | 0.9895低下 | 0度 |
| target+noise | correlation mean | 0.9657 | 0.6636 | 0.3020 | 3.418度 |
| target+noise | steering eta | 0.9567 | 0.0024 | 0.9543 | 2.278度 |
| target+noise | eigenvalue fraction | 0.9667 | 0.6801 | 0.2866 | 3.418度 |
| target+noise | rank-1 residual | 0.1404 | 0.9999 | 0.8595低下 | 1.139度 |

traceはtarget-onlyで正解対遠方margin`-2028`となり、方位選択指標にならない。相関絶対値と固有値集中度は正解近傍で上昇するが、遠方にも高い曖昧peakを持つ。steering etaとrank-1 steering残差はエンドファイアの正解側を明確に分離した。共分散のHermitian相対誤差は`3.35e-8`以下、最小固有値は最大固有値に対して`2.0e-8`以下の負値であり、数値的にHermitian PSDと判定する。

![100 Hz帯長大ULAの共分散指標](../../artifacts/beamforming/low_frequency_long_ula_covariance/covariance_direction_curves.png)

### 8.7 長大等間隔ULAにおける相関集約方式の比較方針

長大ULAでは、誤候補方位に対するchannel間の時間差が基線長とともに増える。一方、単一周波数に近い信号では長基線ほど位相の周期性による曖昧peakを持ち得る。このため、全pairを一つの統計量へ集約する方式だけでなく、物理基線長を分けた統計も同じ共分散から比較する。

各pairには、対角成分を除いた絶対正規化相関

\[
\rho_{ij}(k,\theta)=\frac{|R_{ij}(k,\theta)|}{\sqrt{R_{ii}(k,\theta)R_{jj}(k,\theta)}}
\]

を使用する。複素相関をpair方向へ直接平均しない。比較対象は全pairのmaximum、mean、median、95 percentileと、物理基線を短・中・長に分けたmean、median、95 percentileとする。100 Hzの波長を`15 m`として、短基線を`d<1 lambda`、中基線を`1 lambda<=d<8 lambda`、長基線を`d>=8 lambda`と定義する。基線区分は評価条件であり、実運用APIの固定閾値にはしない。

target-only、noise-only、target+noiseを分離し、2、4、6、8、10秒の完成済み共分散を保存する。各方式について、80--120 Hz帯域平均方位curveから次を評価する。

- source近傍0--2度の平均
- sourceから20度以上離れた方位の平均と最大値
- source近傍対遠方平均margin
- source近傍対最大遠方peak margin
- peak方位誤差
- snapshot時刻間のsource margin標準偏差
- noise-onlyの全方位rangeと最大値

相関値は信号存在とcoherenceの補助指標であり、複素steering整合量の代替とはみなさない。推奨方式は、target条件の平均marginだけでなく、遠方最大peak、noise-onlyの偽選択性、時間安定性を同時に満たすものから選ぶ。maximumはpair数に依存する極値biasを持つため、比較対象には残すが推奨候補から除外する。

#### 8.7.1 比較結果

64ch、間隔6.25 m、開口393.75 m、80--120 Hz広帯域信号について、10秒完成値を比較した。平均marginはsource近傍相関から20度以上遠方の平均相関を引いた値、peak marginはsource近傍相関から遠方最大相関を引いた値である。正値が方位分離を表す。

| scene | 集約方式 | 平均margin | peak margin | peak誤差 |
|---|---|---:|---:|---:|
| target-only | 全pair maximum | 0.0020 | 0.0000 | 0.00度 |
| target-only | 全pair mean | 0.3239 | 0.0140 | 2.28度 |
| target-only | 全pair median | 0.3164 | 0.0094 | 2.28度 |
| target-only | 全pair 95 percentile | 0.0269 | 0.0002 | 0.00度 |
| target-only | 長基線 mean | **0.4412** | **0.0250** | 2.28度 |
| target-only | 長基線 median | 0.4380 | 0.0229 | 2.28度 |
| target-only | 長基線 95 percentile | 0.2110 | 0.0100 | 0.00度 |
| target+noise | 全pair maximum | 0.0029 | -0.0007 | 3.42度 |
| target+noise | 全pair mean | 0.2987 | 0.0204 | 1.14度 |
| target+noise | 全pair median | 0.2895 | 0.0165 | 1.14度 |
| target+noise | 全pair 95 percentile | 0.0336 | 0.0029 | 1.14度 |
| target+noise | 長基線 mean | **0.4006** | **0.0333** | 1.14度 |
| target+noise | 長基線 median | 0.3998 | 0.0300 | 1.14度 |
| target+noise | 長基線 95 percentile | 0.1705 | 0.0110 | 1.14度 |

短基線`d<15 m`はtarget+noiseでpeak誤差31.90度、peak margin`-0.0066`となり、方位選択には不十分であった。候補方位間の時間差が信号自己相関時間に対して小さいためである。中基線meanは平均margin`0.2252`、peak margin`0.0041`で、全pair meanより弱い。

maximumと95 percentileは、誤方位でも高相関となる少数pairを拾うため、平均marginとpeak marginが小さい。medianはmeanに近いが、全pair、長基線ともmeanよりわずかに小さいmarginとなった。noise-onlyでは全方式のsource対遠方平均marginの絶対値は0.012未満であり、0度を特別に選ぶ系統biasは認めなかった。ただし全方位rangeは0.12--0.20あり、10秒・少数snapshotの標本変動は残る。

2、4、6、8、10秒のtarget+noise平均marginは、全pair meanで`0.0003, 0.1873, 0.2831, 0.2910, 0.2987`、長基線meanで`0.0013, 0.2410, 0.3771, 0.3866, 0.4006`となった。2秒では指数積分の有効snapshotが不足し、相関方式を問わず判定に使用できない。10秒でも収束途中であり、実運用閾値の校正には40秒以上の確認を要する。

この条件で相関値から方位選択性を説明する最良の方式は、`d>=8 lambda`の長基線pairに対するmeanである。外れpairへの頑健性を優先する場合は長基線medianを補助表示する。アレイ形状に依存しない全体診断には全pair meanを併記する。maximum、95 percentile、短基線統計は方位選択の採否に使用しない。なお、長基線meanのpeak marginは0.0333に留まり、steering etaのmargin 0.9543より大幅に小さいため、相関方式はWeight主指標ではなく共分散coherenceの診断指標とする。

![相関集約方式の最終方位curve](../../artifacts/beamforming/long_ula_correlation_method_comparison/target_plus_noise_final_direction_curves.png)

![相関marginの時間収束](../../artifacts/beamforming/long_ula_correlation_method_comparison/target_plus_noise_margin_convergence.png)

### 8.8 信号周波数・帯域幅の比較方針

60度信号について、`40--60 Hz`、`80--120 Hz`、`160--240 Hz`の広帯域信号と、`100 Hz`単一toneを同じ64ch、間隔6.25 m、開口393.75 mのULAへ入力する。`fs=1024 Hz`、`NFFT=256`、積分時間10秒、target+noiseを共通条件とする。4 Hz FFT binにより100 Hz toneをbin中心へ一致させ、leakageによる見かけの広帯域化を避ける。

各広帯域条件では中心周波数`f_c`に対する波長`lambda_c=c/f_c`を用いて、短基線`d/lambda_c<1`、中基線`1<=d/lambda_c<8`、長基線`d/lambda_c>=8`を再定義する。物理距離を固定した区分とせず、周波数が変わっても同じ波長正規化基線を比較する。

同一アレイのまま周波数を変えるため、160--240 Hzでは受波器間隔6.25 mが120 Hzを超えて半波長条件を満たさない。したがって同条件の曖昧peakには、信号帯域・自己相関時間の効果だけでなく空間aliasも含まれる。40--60 Hzでは空間aliasはないが、同じ物理開口を波長で測った有効開口が小さくなる。100 Hz toneでは時間移動によって絶対相関が低下しない理論を確認し、広帯域3条件と別に解釈する。

各条件について、全pair maximum、mean、median、95 percentile、および短・中・長基線のmean、median、95 percentileを同一方位軸・相関値範囲で描画する。60度近傍、20度以上遠方、遠方最大peak、peak誤差をJSONへ保存する。

#### 8.8.1 比較結果

target+noiseを10秒積分した完成値について、方位選択性が最も高かった長基線meanと、アレイ全体を表す全pair meanを比較した。

| 信号 | 空間alias free | 全pair mean平均margin | 全pair mean peak margin | 長基線mean平均margin | 長基線mean peak margin | 長基線mean peak誤差 |
|---|---|---:|---:|---:|---:|---:|
| 40--60 Hz広帯域 | yes | 0.1971 | 0.0428 | **0.3989** | **0.1193** | 0.38度 |
| 80--120 Hz広帯域 | yes | 0.2055 | 0.0362 | **0.3195** | **0.0655** | 0.76度 |
| 160--240 Hz広帯域 | no | 0.2106 | 0.0694 | **0.2653** | **0.0906** | 0.38度 |
| 100 Hz単一tone | yes | 0.0004 | -0.0002 | 0.0003 | -0.0002 | 96.08度 |

40--60 Hzでは長基線を`d>=240 m`、80--120 Hzでは`d>=120 m`、160--240 Hzでは`d>=60 m`としており、いずれも中心周波数で`d/lambda>=8`となるpairを選択した。広帯域3条件では長基線meanが全pair meanより高いmarginを示し、入力60度近傍にpeakを形成した。

広帯域間のmarginは中心周波数に対して単調増加しなかった。波長正規化基線のpair構成、絶対帯域幅、各候補方位の更新snapshot、160--240 Hzに含まれる空間aliasが同時に変化するためである。特に160--240 Hzは中心200 Hzで`d/lambda=0.833`となり半波長条件を超える。帯域平均curveでは60度peakを維持したが、alias-freeな方式の校正条件には使用しない。

100 Hz toneでは、全pair・全基線区分の相関がほぼ1であり、平均marginは`4e-4`以下、peak方位は正解と一致しなかった。単一toneは時間移動しても振幅相関が低下せず、絶対正規化相関が位相を捨てるためである。したがって絶対相関による方位選択性は「信号周波数が高いほど成立する」のではなく、有限の帯域幅と、誤候補方位で生じる時間差が信号自己相関を低下させる場合にのみ成立する。

![40--60 Hz相関方位curve](../../artifacts/beamforming/long_ula_frequency_correlation_comparison/broadband_40_60_hz_direction_curves.png)

![80--120 Hz相関方位curve](../../artifacts/beamforming/long_ula_frequency_correlation_comparison/broadband_80_120_hz_direction_curves.png)

![160--240 Hz相関方位curve](../../artifacts/beamforming/long_ula_frequency_correlation_comparison/broadband_160_240_hz_direction_curves.png)

![100 Hz tone相関方位curve](../../artifacts/beamforming/long_ula_frequency_correlation_comparison/tone_100_hz_direction_curves.png)

### 8.9 全pair相関中央値とsteering powerの比較方針

steering powerとの主比較には、長基線だけを選別した相関中央値ではなく、全有効channelの下三角全pair中央値を使用する。長基線限定は基線選択という追加条件を持ち、全channelを使用するsteering powerとの比較が非対称になるためである。

主比較指標を次の2量に限定する。

\[
\rho_{\mathrm{median}}(k,\theta)
=
\operatorname{median}_{i>j}
\frac{|R_{ij}(k,\theta)|}{\sqrt{R_{ii}(k,\theta)R_{jj}(k,\theta)}}
\]

\[
\eta(k,\theta)
=
\frac{P_{\mathrm{steering}}(k,\theta)}{P_{\mathrm{total}}(k,\theta)}
\]

両者は0--1の無次元比だが、同じ物理量ではない。絶対値の大小を直接比較せず、方位curveのpeak誤差、source対遠方平均margin、source対遠方最大peak margin、peak幅、noise-onlyの偽peak、時間収束を比較する。steering powerは全channel係数1を使用し、相関中央値と同じsnapshot、FFT、忘却係数で指数積分する。

評価sceneはtarget-only、noise-only、target+noiseを分離する。周波数条件は40--60 Hz、80--120 Hz、160--240 Hz、100 Hz単一tone、信号方位60度とし、2、4、6、8、10秒の完成境界で保存する。基線別中央値は主比較から外し、結果を説明する補助診断だけに残す。

#### 8.9.1 比較結果

target+noiseの10秒完成値を次に示す。半高幅は各curveの最小値と最大値の中点を閾値とし、sourceを含む連続領域の幅とした。

| 信号 | 指標 | 平均margin | peak margin | peak誤差 | 半高幅 |
|---|---|---:|---:|---:|---:|
| 40--60 Hz | 全pair相関median | 0.1308 | 0.0425 | 0.38度 | 83.16度 |
| 40--60 Hz | steering power eta | **0.7877** | **0.7853** | 0.38度 | **3.42度** |
| 80--120 Hz | 全pair相関median | 0.1670 | 0.0196 | 0.38度 | 105.95度 |
| 80--120 Hz | steering power eta | **0.4707** | **0.4696** | 0.38度 | **1.14度** |
| 160--240 Hz | 全pair相関median | 0.1735 | 0.0398 | 0.38度 | 94.56度 |
| 160--240 Hz | steering power eta | **0.2241** | **0.1896** | 0.38度 | **1.14度未満** |
| 100 Hz tone | 全pair相関median | -0.0001 | -0.0005 | 80.13度 | 36.46度 |
| 100 Hz tone | steering power eta | **0.4891** | **0.4867** | 0.38度 | **1.14度** |

全pair相関medianは広帯域3条件で60度近傍が高くなるが、半高幅は83--106度と広く、遠方最大peakに対するmarginは0.02--0.04に留まった。100 Hz toneのtarget-onlyでは全方位が厳密に1となり、方位選択性を持たない。target+noiseでもmarginはほぼ0である。
tone相関medianの半高幅36.46度は、range約0.0011のnoise揺らぎに半高定義を適用した値であり、mainlobe幅として物理的意味を持たない。

steering power etaは4条件すべてで60度にpeakを形成した。100 Hz toneでも複素位相を保持するため、絶対相関medianが失った方位情報を維持した。160--240 Hzでは120度以上に空間alias由来の小peakが現れるが、正解peakに対するmarginは0.1896を保った。ただし同条件をalias-freeな閾値校正には使用しない。

target+noiseの2、4、6、8、10秒における平均marginは、40--60 Hzの相関medianで`-0.0020, 0.0615, 0.1229, 0.1264, 0.1308`、steering powerで`0.7641, 0.7759, 0.7823, 0.7885, 0.7877`であった。相関medianは共分散の有限snapshot biasと収束に依存する一方、steering powerは2秒の最初の完成周期から安定した。100 Hz toneでもsteering powerは全時刻で約0.489のmarginを維持した。

noise-onlyの全pair相関medianは有限snapshot biasにより約0.44となった。steering power etaは概ね空間白色雑音の理論基準`1/N_ch=1/64=0.015625`付近にあり、方位rangeも相関medianより小さい。以上から、全pair相関medianは共分散coherenceを説明する補助診断として保持し、方位Weightの主指標にはsteering power etaを使用する。

![40--60 Hzのmedianとsteering power比較](../../artifacts/beamforming/correlation_median_steering_power_comparison/broadband_40_60_hz_comparison.png)

![80--120 Hzのmedianとsteering power比較](../../artifacts/beamforming/correlation_median_steering_power_comparison/broadband_80_120_hz_comparison.png)

![160--240 Hzのmedianとsteering power比較](../../artifacts/beamforming/correlation_median_steering_power_comparison/broadband_160_240_hz_comparison.png)

![100 Hz toneのmedianとsteering power比較](../../artifacts/beamforming/correlation_median_steering_power_comparison/tone_100_hz_comparison.png)

### 8.10 分析幅粗さに対するsteering powerとMVDRの分離評価

長大ULAでは、steering powerの鋭いpeakが残ることと、その方位別共分散を単一steering vector前提のMVDRへ使用できることを分離する。64ch、間隔6.25 m、開口393.75 m、`fs=32768 Hz`とし、`delta_f=1,4,16,64,256 Hz`、source方位`0,30,60,90,120,150,180度`を比較する。

平坦な1 bin広帯域信号について、同一時間blockのbin積分共分散はpair遅延`tau_ij`に対して`np.sinc(delta_f*tau_ij)`のcoherence低下を持つ。候補方位別時間切り出しでは残留遅延`tau_true-tau_candidate`を使用する。正しい候補で残留遅延が0ならrank 1が回復するが、同一時間blockではendfireほどrankが増える。単一toneはbin中心へ一致させ、sinc低下を持たない。

全channel pair相関median、直接射影と数学的に同値なeta、Hermitian/PSD/trace/固有値、固定loading MVDR/Caponを同じモデル共分散から計算する。方位別時間切り出しあり・同一時間blockを分離し、1 Hz条件は現行1秒schedule不成立と延長window参照を両方記録する。

失敗判定は、source peak誤差2度超、source対最大遠方peak marginが0以下、半高幅の基準比2倍超、MVDR distortionless応答誤差0.5 dB超、または最大不要peakがsource peak以上とする。物理的grating lobeは分析幅破綻と分けて記録する。

#### 8.10.1 結果と失敗境界

同一時間blockではendfireを含む90度以外の方位でMVDR幅が4 Hzから劣化し、steering etaは16 Hzからpeak/margin/幅条件を満たさなくなった。256 Hzでは0/180度のetaとMVDRがともに57.5度の誤peakを選び、明確に破綻した。90度はchannel間遅延が0なので、この狭帯域近似破綻を起こさなかった。

候補方位別時間切り出しでは、`delta_f=256 Hz`を含む全条件でsteering etaとMVDRのpeak誤差は0度であった。256 Hzのeta最大遠方marginは0.9675--0.9681、MVDR最大遠方marginは0.9279--0.9961である。正解候補共分散はHermitian PSDで、固定loading MVDRのtarget応答は0 dBを維持した。

従って本評価の分類は次のとおりである。

- 同一時間block: MVDR幅が先に4 Hzで悪化し、steering peakは16 Hzで後から破綻する。endfire側がbroadsideより先に悪化する。
- 方位別時間切り出し: 256 Hzでも鋭いsteering peakが残り、同じ正解候補共分散を使用したMVDRも成立する。
- 1 Hz: 参照計算自体は成立するが、1秒window＋262.5 ms遅延開口を現行1秒scheduleへ収められない。物理遅延をscaleせず成立不能とする。
- 256 Hz: FFT bin中心が256 Hzとなるため、間隔6.25 mの空間aliasを含む。time-cut broadbandの候補依存coherenceが曖昧候補を抑えたが、alias-free校正条件ではない。

![0度sourceの分析幅比較](../../artifacts/beamforming/analysis_width_long_array_mvdr/overlay_source_0_deg.png)

![90度sourceの分析幅比較](../../artifacts/beamforming/analysis_width_long_array_mvdr/overlay_source_90_deg.png)

![180度sourceの分析幅比較](../../artifacts/beamforming/analysis_width_long_array_mvdr/overlay_source_180_deg.png)

![steering eta margin heatmap](../../artifacts/beamforming/analysis_width_long_array_mvdr/heatmap_time_cut_eta_source_far_peak_margin.png)

![MVDR margin heatmap](../../artifacts/beamforming/analysis_width_long_array_mvdr/heatmap_time_cut_mvdr_source_far_peak_margin.png)

## 9. 指標の分類

| 指標 | 分類 | Weightへの使用 |
|---|---|---|
| trace | 共分散の合計power | 方位Weightに使用しない |
| correlation mean/median/95 | coherence・信号存在の補助説明 | 診断のみ |
| correlation maximum | 極値biasを含む比較値 | 採否に使用しない |
| 物理基線・d/λ別相関 | 長基線のcoherenceと曖昧性 | 診断・grating監視 |
| 最大固有値比・gap | rank-1信号空間の集中度 | 補助診断のみ |
| η | 候補steeringとの複素位相整合 | Weight主指標 |
| rank-1 steering残差 | 候補rank-1 modelとの不一致 | ηの補助診断 |

検証仮説に対する結論は次のとおりである。

> 「正しい候補方位で方位別共分散powerまたは絶対相関が大きくなる」という前提は成立しない。方位別遅延snapshot方式の方位選択は、共分散の強弱ではなく、候補steeringとの複素位相整合性で行う。

## 10. Weight設計への反映

Weightの主指標をηとする。trace、絶対相関、固有値集中度を直接Weightへ使用しない。

補助成立条件は次とする。

- total powerが数値下限を超える
- ηが有限かつ物理範囲内である
- 完成共分散が有限である
- Hermitian誤差が許容範囲内である
- 半正定値性が許容範囲内である

soft weight候補は次式を維持する。

\[
g(k,\theta)
=
\operatorname{clip}
\left(
\frac{\eta(k,\theta)-\gamma_{\mathrm{off}}(k)}
{\gamma_{\mathrm{on}}(k)-\gamma_{\mathrm{off}}(k)},
0,1
\right)
\]

`γ_off(k)`はnoise-onlyの周波数別分布と許容誤検出率、`γ_on(k)`はtarget sceneの検出率から校正する。既存評価のq99/q10は候補であり、固定値として採用しない。

Weight総和0、非有限、実効方位数不足、共分散品質不成立の場合は方式2共分散へfallbackする。T=40 sとT=128 sで有効snapshot数を増やし、未使用方位・周波数・SNRを含むROC確認後に閾値を確定する。

## 11. 演算量と実測

64ch、159方位、65binの場合の理論演算量は次のとおりである。

| 計算 | complex MAC | 相対量 |
|---|---:|---:|
| 完成共分散への`u^H R u` | 42,332,160 | 64 |
| FFT直後の`u^H X` | 661,440 | 1 |

固定配列を5回測定した結果は次のとおりである。

| 計算 | 中央値 | 最大 |
|---|---:|---:|
| 完成共分散二次形式 | 4.74 ms | 28.24 ms |
| 直接snapshot射影 | 0.236 ms | 0.319 ms |

比較用に完成共分散を明示生成した配列は338,657,280 byte、直接射影出力は82,680 byteであった。実際の1秒入力処理ではsteering整合量の有効化による中央値増加は約1.1 msである。

共分散自体はMVDR、固有値評価、相関統計に必要なため保持する。ただし、Weight生成だけを目的として完成共分散へ二次形式を再計算しない。

## 12. 実装・成果物対応

| 責務 | 配置 |
|---|---|
| trace、固有値集中度、steering整合量、rank-1残差 | `src/spflow/beamforming/covariance_subspace_metrics.py` |
| 非対角相関、物理基線、d/λ、pair構成統計 | `src/spflow/beamforming/spatial_correlation_statistics.py` |
| 中心sample切り出し、時間軸復元、方位別共分散積分 | `src/spflow/beamforming/covariance_snapshot_schedule.py` |
| 6 scene評価、画像、数値比較、演算時間計測 | `evaluations/beamforming/method3_covariance_directionality.py` |
| 数値成果物 | `artifacts/beamforming/method3_covariance_directionality/summary.json` |
| scene別配列・画像 | `artifacts/beamforming/method3_covariance_directionality/` |

本書は`共分散積分3方式比較設計.md`の6.11章を独立して読める形に展開したものである。方位別遅延snapshot方式全体の周期、指数積分係数、共分散合成、fallback順序は元設計書を正とする。
